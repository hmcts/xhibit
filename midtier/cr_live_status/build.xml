<?xml version="1.0" encoding="UTF-8"?>

<project basedir="." default="all" name="CrLiveStatus">

    <description>
        The CrLiveStatus sub-project.  This is used to populate the
        xhb_cr_live_status table for use by the internet pages.
    </description>

    <import file="./../../tools/ant/framework.xml" />

    <property name="project.type.builds.jar"           value="marker" />
    <property name="project.type.requires.compilation" value="marker" />

    <!--
      =======================================================================
        Initialise Override
      =======================================================================
      -->
    <target name="initialise" unless="skip.initialise" depends="framework.initialise">
        <log target="initialise"/>
        <path id="compile.classpath">
            <path refid="lib.classpath"/>
            <fileset dir="${build.dest.dir}">
                <include name="Framework.jar" />
                <include name="XhibitValues.jar" />
                <include name="XhibitEntities.jar" />
                <include name="CourtLogCommon.jar" />
            </fileset>
        </path>

        <!-- Indicate that the jar file create by this class should be included in the WAR file -->
        <property name="prerelease.includes" value="CourtLogCommon.jar, CrLiveStatus.jar" />
    </target>

</project>
